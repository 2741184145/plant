# 核心深度学习框架 (锁定 2.10.0，这是最后一个支持 Windows 原生 GPU 的版本)
tensorflow==2.10.0

# 矩阵计算 (强制限制在 1.x 版本，以兼容旧版 TensorFlow)
numpy<2.0.0

# 计算机视觉库 (用于处理图像缩放、提取超绿特征和掩码生成)
opencv-python

# 数据可视化 (用于绘制训练过程的 Loss 曲线、IoU 曲线和预测对比图)
matplotlib